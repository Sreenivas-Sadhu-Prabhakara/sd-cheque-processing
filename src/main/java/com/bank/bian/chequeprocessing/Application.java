package com.bank.bian.chequeprocessing;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Cheque Processing
 *
 * Business Area:      Operations and Execution
 * Business Domain:    Payments
 * Functional Pattern: Process
 * Asset Type:         Cheque Transaction
 * Control Record:     Cheque Transaction Procedure
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Cheque Processing — BIAN Semantic API")
                .description("Service Domain 'Cheque Processing' (Process pattern) "
                        + "operating on the 'Cheque Transaction Procedure' control record.")
                .version("0.1.0"));
    }
}
